= eval(string)

// metadata for AI
ifdef::ai[]
Summary: Script reference for the eval(string) global function, which evaluates a dynamically-created string of JavaScript code. Use sparingly as eval-dependent scripts are difficult to debug.
endif::ai[]
:navtitle: eval()


Evaluate a string of JavaScript code. This function can be useful for executing dynamically-created JavaScript commands and expressions, but scripts that rely on `eval()` are often difficult to debug. In general, you should avoid using `eval()`.