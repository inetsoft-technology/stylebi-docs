= LinearScale.setScaleRange(range)
:navtitle: setScaleRange()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LinearScale.setScaleRange(range)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:Scale.adoc[]
====

Specifies the calculation strategy for finding the scale range.  The corresponding “getter” function is `getScaleRange()`.

== Parameter
range::			a xref:ScaleRange.adoc[] object




== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LinearScaleSetScaleRange1.js[]
----

<1> {dataset}
<2> Create the xref:LinearScale.adoc[] object.
<3> Use `setScaleRange` to compute the scale range by stacking.
<4> Use xref:EGraphSetScale.adoc[] to assign the `Scale` to the Chart.

image::commonscript:LinearScaleSetRange.png[alt="bar chart with stacked scale range"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


