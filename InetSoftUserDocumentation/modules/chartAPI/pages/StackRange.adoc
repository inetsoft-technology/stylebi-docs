= StackRange
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `StackRange`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:ScaleRange.adoc[]
====

The `StackRange` object computes the range by “stacking” the data values. To create a `StackRange` object, call the `StackRange` constructor.

[source,javascript]
var range = new StackRange();


== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$StackRange1.js[]
----
<1> {dataset}
<2> Create the xref:Scale.adoc[] object.
<3> Create the xref:StackRange.adoc[] object and assign to the `Scale`.
<4> Use xref:EGraphSetScale.adoc[] to add the `Scale` to the Chart.

image::commonscript:LinearScaleSetRange_1.png[alt="bar chart with Y-axis range set by stacking NJ and NY values"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

