= StackRange.setGroupField(value)
:navtitle: setGroupField()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `StackRange.setGroupField(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:ScaleRange.adoc[]
====

Determines the scale range from the stacked values of largest single group, based on the specified grouping field.  The corresponding “getter” function is `getGroupField()`.

== Parameter
value::			the field name, a String


== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$StackRangeSetGroupField1.js[]
----
<1> {dataset}
<2> Create the xref:Scale.adoc[] object.
<3> Create the xref:StackRange.adoc[] object.
<4> Use `setGroupField` to compute the stacking based on the '`State`' field.
<5> Assign the `ScaleRange` to the `Scale`.
<6> Use xref:EGraphSetScale.adoc[] to add the `Scale` to the Chart.

image::commonscript:ScaleRangeSetGroup.png[alt="point chart with scale range computed by stacking grouped State values"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

