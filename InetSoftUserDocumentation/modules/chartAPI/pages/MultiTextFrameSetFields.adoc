= MultiTextFrame.setFields(arr)
:navtitle: setFields()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `MultiTextFrame.setFields(arr)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====

Specifies the columns containing the text specifications.  The corresponding “getter” function is `getFields()`.



== Parameter
arr::			Array of Strings






== Example

.Chart Component Script
[source,javascript]
----
dataset = [["State", "Name", "Quantity"], ["NJ", "New Jersey", 200], ["NY", "New York", 300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var mtframe = new MultiTextFrame(); //<2>
mtframe.setFields("Name","Quantity"); //<3>
elem.setTextFrame(mtframe); //<4>
graph.addElement(elem);
----

<1> {dataset}
<2> Create   the `MultiTextFrame` object.
<3> Use `setFields` to specify the fields for the `MultiTextFrame`.
<4> Use xref:GraphElementSetTextFrame.adoc[] to add the `MultiTextFrame` to the xref:IntervalElement.adoc[].

image::commonscript:MultiTextFrameSetField.png[alt="bar chart with multiple text field labels displayed"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]




// use delimiter to concatenate the values, or set message format.
//text.setDelimiter("\n");
