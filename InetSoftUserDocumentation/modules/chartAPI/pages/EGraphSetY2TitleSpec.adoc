= EGraph.setY2TitleSpec(spec)
:navtitle: setY2TitleSpec()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `EGraph.setY2TitleSpec(spec)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the Y-axis title information for Y-axis to the *right* of the  Chart.  The corresponding “getter” function is xref:chartAPI:EGraphGetY2TitleSpec.adoc[].

== Parameter
spec::			A xref:chartAPI:TitleSpec.adoc[] object







== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$EGraphSetY2TitleSpec1.js[]
----
<1> {dataset}
<2> Create the xref:TitleSpec.adoc[] object.
<3> Use `setY2TitleSpec` to add the `TitleSpec` to Chart.


image::commonscript:EGraphaddsetY2TitleSpec.png[alt="EGraph with Y2 title spec set"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



[TIP]
=====

To change the property on a Chart that was previously created with the Chart Editor, use xref:chartAPI:EGraphGetY2TitleSpec.adoc[] ① to get a handle to the `TitleSpec` object. For example:


[source,javascript]
----
var spec = graph.getY2TitleSpec();
spec.setLabel("Y Title");
----
=====



