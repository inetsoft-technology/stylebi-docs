= AxisSpec.setLabelGap(value)
:navtitle: setLabelGap()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `AxisSpec.setLabelGap(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the gap between the axis labels and the axis line.  The corresponding “getter” function is `getLabelGap()`.

== Parameter
value:: 			integer number of pixels to use for gap



== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$AxisSpecSetLabelGap1.js[]
----
<1> {dataset}
<2> Create the `AxisSpec` object.
<3> Use `setLabelGap` to add space next to the axis labels.

image::commonscript:TextSpecsetLabelGap.png[alt="axis label gap setting example"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use  xref:chartAPI:EGraphGetCoordinate.adoc[] ①, xref:chartAPI:RectCoordGetYScale.adoc[] ②,  and xref:chartAPI:ScaleGetAxisSpec.adoc[] ③ to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object, xref:chartAPI:Scale.adoc[] object, and so on. For example:

[source,javascript]
----
include::scriptexample:partial$AxisSpecSetLabelGap2.js[]

----
=====