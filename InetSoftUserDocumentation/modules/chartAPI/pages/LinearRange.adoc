=  LinearRange
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LinearRange`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:ScaleRange.adoc[]
====

The `LinearRange` object computes the range by using the minimum and maximum data values. To create a LinearRange object, call the `LinearRange` constructor.

[source,javascript]
var range = new LinearRange();



== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LinearRange1.js[]
----

<1> {dataset}
<2> Create the `LinearRange` object.
<3> Use `setScaleRange` to assign the `LinearRange` to the xref:LinearScale.adoc[].
<4> Use  xref:EGraphSetScale.adoc[] to add the Scale to the Chart.


image::commonscript:LinearRange.png[alt="bar chart with linear range computed from data min and max"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

