= PowerLineEquation
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `PowerLineEquation`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies a power data fit for a Chart.


== Example

.Chart Component Script
[source,javascript]
----
var form = new LineForm(); //<1>
var equation = new PowerLineEquation(); //<2>
form.setLineEquation(equation); //<3>
form.setColor(java.awt.Color(0xff0000));
graph.addForm(form); //<4>
----
<1> Create a  xref:LineForm.adoc[] object.
<2> Create the `PowerLineEquation`.
<3> Use    xref:GraphFormSetLineEquation.adoc[] to apply a the `PowerLineEquation`.
<4> Use xref:EGraphAddForm.adoc[] to add the `LineForm` to the Chart.

image::commonscript:PowerLineEquation.png[alt="power line equation fit on chart"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


