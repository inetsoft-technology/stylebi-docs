= LegendSpec.setVisible(Boolean)
:navtitle: setVisible()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LegendSpec.setVisible(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies whether the legend is visible.  The corresponding “getter” function is `getVisible()`.




== Parameter
Boolean::			`true`: show legend +
`false`: hide legend







== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LegendSpecSetVisible1.js[]
----
<1> {dataset}
<2> Create the xref:LegendSpec.adoc[] object.
<3> Use `setVisible` to hide  the legend.
<4> Use  xref:VisualFrameSetLegendSpec.adoc[] to assign the `LegendSpec` to the `ColorFrame`.

image::commonscript:LegendSpecsetVisible.png[alt="chart with legend hidden"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====

To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:chartAPI:EGraphGetElement.adoc[] {circle1}, xref:chartAPI:GraphElementGetColorFrame.adoc[] {circle2}, and xref:chartAPI:VisualFrameGetLegendSpec.adoc[] {circle3} to obtain a handle to the desired xref:chartAPI:GraphElement.adoc[],  xref:chartAPI:ColorFrame.adoc[], and xref:chartAPI:LegendSpec.adoc[]. For example:

[source,javascript]
----
var elem = graph.getElement(0); // <1>
var frame = elem.getColorFrame(); // <2>
var spec = frame.getLegendSpec(); // <3>
// Compact syntax: var spec = graph.getElement(0).getColorFrame().getLegendSpec();

spec.setVisible(false);

----
=====