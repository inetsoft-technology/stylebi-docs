= GShape.ImageShape.setTile(Boolean)
:navtitle: setTile()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GShape.ImageShape.setTile(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies whether the image should be stretched to fit the fill area or tiled at the original size.  You can also set this using the `ImageShape.tile`  property syntax in *onRefresh* script. See xref:#Example2[] below.





== Parameter
Boolean:: `true`: keep original size, and tile to fit +
`false`: stretch to fit (default)




== Example 1

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GShapeImageShapeSetTile1.js[]
----
<1> {dataset}
<2> Create the `GShape.ImageShape` shape.
<3> Use `setTile()` to tile the image.
<4> Pass the `shape` to the xref:StaticShapeFrame.adoc[] constructor.
<5> Use xref:GraphElementSetShapeFrame.adoc[] to add the `ShapeFrame` to the xref:IntervalElement.adoc[].
+
image::commonscript:GShapeImageShape2tile.png[alt="bar chart with tiled image shape filling bar elements"]
+
include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



[TIP]
=====

To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:EGraphGetElement.adoc[] {circle1} to obtain a handle to the desired xref:GraphElement.adoc[] object. For example:

[source,javascript]
----
var elem = graph.getElement(0); //<1>
var logo = getImage("https://www.inetsoft.com/images/home/logo.gif");
var shape = new GShape.ImageShape();
shape.setImage(logo);
shape.setTile(true);
var frame = new StaticShapeFrame(shape);
elem.setShapeFrame(frame);
----
=====



[#Example2]
== Example 2



This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ShapeFrame.adoc[].

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
include::scriptexample:partial$GShapeImageShapeSetTile2.js[]
----

+
image::commonscript:GShapeImageTile.png[alt="point chart with tiled image shape via bindingInfo shapeFrame property"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

