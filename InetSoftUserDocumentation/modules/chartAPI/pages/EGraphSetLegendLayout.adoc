= EGraph.setLegendLayout(value)
:navtitle: setLegendLayout()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `EGraph.setLegendLayout(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[WARNING,caption=see also]
xref:dashboardscript:ChartSetActionVisible.adoc[] to control the ability of a user to position the legend and to access  other Chart features.

Specifies the Chart legend’s position.  The corresponding “getter” function is xref:chartAPI:EGraphGetLegendLayout.adoc[].





== Parameter
value:: One of the following constants:
+
include::dashboardscript:partial$legendPosition.adoc[]

== Example
.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"], ["NJ",200], ["NY",300]];
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var frame = new CategoricalColorFrame();
frame.setField("State");
elem.setColorFrame(frame);
graph.setLegendLayout(Chart.BOTTOM); <1>
graph.addElement(elem);
----
<1> Set the legend layout.

image::commonscript:EGraphaddsetLegendLayout.png[alt="EGraph with legend layout set to bottom"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====
To use the property on a Chart that was previously created with the Chart Editor, simply call the function. For example:

[source,javascript]
----
graph.setLegendLayout(Chart.BOTTOM);
----
=====



