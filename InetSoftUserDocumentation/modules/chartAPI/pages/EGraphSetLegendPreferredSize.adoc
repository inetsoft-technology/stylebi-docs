= EGraph.setLegendPreferredSize(value)
:navtitle: setLegendPreferredSize()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `EGraph.setLegendPreferredSize(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the Chart legend’s height or width in pixels.  The corresponding “getter” function is xref:chartAPI:EGraphGetLegendPreferredSize.adoc[].

== Parameter
value:: Integer number of pixels





== Example
.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"], ["NJ",200], ["NY",300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var frame = new CategoricalColorFrame();
frame.setField("State");
elem.setColorFrame(frame);
graph.setLegendPreferredSize(150); //<2>
graph.addElement(elem);
----
<1> {dataset}
<2> Use `setLegendPreferredSize()` to set the legend size.

image::commonscript:EGraphaddsetLegendPreferredSize.png[alt="EGraph with legend preferred size set"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====

To use the property on a Chart that was previously created with the Chart Editor, simply call the function. For example:

[source,javascript]
----
graph.setLegendPreferredSize(150);
----
=====



