= AxisSpec.setLabelOnSecondaryAxis(Boolean)
:navtitle: setLabelOnSecondaryAxis()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

Specifies whether axis labels are displayed on the opposite side of the chart. For a Y-axis, labels move from left to right; for an X-axis, labels move from bottom to top. {noGetter}

== Parameter
Boolean:: `true`: display labels on the opposite side +
`false`: display labels on the default side (default)

== Example

.Chart Component Script
[source,javascript]
--
include::scriptexample:partial$AxisSpecSetLabelOnSecondaryAxis1.js[]
--
<1> {dataset}
<2> Create the `AxisSpec` object.
<3> Use `setLabelOnSecondaryAxis` to move axis labels to the opposite side of the chart.

image::commonscript:AxisSpecSetLabelOnSecondaryAxis.png[alt="bar chart with axis labels displayed on the opposite side"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use xref:chartAPI:EGraphGetCoordinate.adoc[] ①, xref:chartAPI:RectCoordGetYScale.adoc[] ②, and xref:chartAPI:ScaleGetAxisSpec.adoc[] ③ to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object, xref:chartAPI:Scale.adoc[] object, and so on. For example:

[source,javascript]
--
include::scriptexample:partial$AxisSpecSetLabelOnSecondaryAxis2.js[]
--
=====
