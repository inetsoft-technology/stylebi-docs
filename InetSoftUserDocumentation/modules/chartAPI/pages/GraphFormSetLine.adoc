= GraphForm.setLine(value)
:navtitle: setLine()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GraphForm.setLine(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the line style used to draw the form.  The corresponding “getter” function is `getLine()`.


== Parameter
value::					A xref:GLine.adoc[] constant:
+
include::partial$gLineConstants.adoc[]



== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GraphFormSetLine1.js[]
----
<1> {dataset}
<2> Create the  xref:GraphForm.adoc[] object.
<3> Use `setLine()` to set the line style.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:GraphFormSetLine.png[alt="chart with dash line style applied to form"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

