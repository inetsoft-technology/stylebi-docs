= Scale.setFields(field)
:navtitle: setFields()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `Scale.setFields(field)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the fields to which the xref:Scale.adoc[] should be applied.  The corresponding “getter” function is `getFields()`.

== Parameter
field::			a String containing a column name


== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$ScaleSetFields1.js[]
----

<1> {dataset}
<2> Create   the xref:Scale.adoc[] object.
<3> Use `setFields` to apply the  `Scale` to the '`Quantity`' field.
<4> Use the `Scale` to create a new xref:Coordinate.adoc[].
<5> Use xref:EGraphSetCoordinate.adoc[] to add the `Coordinate` to the Chart.

image::commonscript:ScaleFields.png[alt="bar chart with LogScale applied to Quantity field via setFields"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


