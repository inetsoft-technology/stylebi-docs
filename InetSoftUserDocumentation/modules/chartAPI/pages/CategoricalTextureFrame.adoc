= CategoricalTextureFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `CategoricalTextureFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====

The `CategoricalTextureFrame` object contains a texture for visual chart objects, rendering a unique texture for each discrete value. To create a `CategoricalTextureFrame` object, call the `CategoricalTextureFrame` constructor.

[source,javsscript]
var frame = new CategoricalTextureFrame('State');

You can pass the name of a field (e.g., ‘State’) to the constructor, or specify this later using the inherited xref:VisualFrameSetField.adoc[] property. You can also assign a `TextureFrame` using the xref:dashboardscript:ChartBindingInfoTextureFrame.adoc[] property in *onRefresh* script. See xref:#Example2[] below.

`CategoricalTextureFrame` provides the special xref:chartAPI:CategoricalTextureFrameSetTexture.adoc[] method.


== Example 1

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$CategoricalTextureFrame1.js[]
----
<1> {dataset}
<2> Create the xref:CategoricalTextureFrame.adoc[] object.
<3> Use xref:GraphElementSetTextureFrame.adoc[] to add the `CategoricalTextureFrame` to the xref:IntervalElement.adoc[].

image::commonscript:CategoricalTextureFrame2.png[alt="bar chart with unique texture per category value"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:EGraphGetElement.adoc[] {circle1} to obtain a handle to the desired xref:GraphElement.adoc[] object. For example:

[source,javascript]
----
var elem = graph.getElement(0); //<1>
var frame = new CategoricalTextureFrame();
frame.setField("Rep");
elem.setTextureFrame(frame);

----
=====


[#Example2]
== Example 2


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:TextureFrame.adoc[].

. Bind a xref:dashboard:BarChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
include::scriptexample:partial$CategoricalTextureFrame2.js[]
----

+
image::commonscript:CategoricalTextureFrameSetTexture.png[alt="bar chart with texture frame applied via bindingInfo"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

