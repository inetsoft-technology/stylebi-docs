= LineForm.addValues(value)
:navtitle: addValues()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LineForm.addValues(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====

Specifies a point defining the line in logical space. The coordinates of the points are relative to prevailing axis scaling prior to transformation. So, for a categorical X-axis (e.g., ‘NJ’, ‘NY’, ‘PA’, etc.), the X-values of points should specify a categorical value (e.g., ‘NJ’).






== Parameter
value::			An [X,Y] pair

== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LineFormAddValues1.js[]
----
<1> {dataset}
<2> Create the  xref:LineForm.adoc[] object.
<3> Use `addValues()` to set `form` location.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:LineFormAddValues.png[alt="bar chart with line form using categorical axis values"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

