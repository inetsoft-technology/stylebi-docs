= GraphForm.setColor(value)
:navtitle: setColor()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GraphForm.setColor(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the line and fill color of the xref:GraphForm.adoc[].  The corresponding “getter” function is `getColor()`.





== Parameter
value::
A {javaawtcolor} object




== Example


.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"], ["NJ", 200], ["NY", 300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var form = new LineForm(); //<2>
form.addPoint(new java.awt.Point(0,0));
form.addPoint(new java.awt.Point(100,100));
form.addPoint(new java.awt.Point(200,100));
form.setColor(java.awt.Color(0xff0000)); //<3>
graph.addForm(form); //<4>
graph.addElement(elem);
----
<1> {dataset}
<2> Create the  xref:GraphForm.adoc[] object.
<3> Use `setColor` to assign the color to the `GraphForm`.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:GraphFormSetColor.png[alt="chart with line form color set to red"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

