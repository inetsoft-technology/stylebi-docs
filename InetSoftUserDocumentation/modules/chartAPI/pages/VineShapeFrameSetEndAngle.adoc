= VineShapeFrame.setEndAngle(value)
:navtitle: setEndAngle()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `VineShapeFrame.setEndAngle(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:MultiShapeFrame.adoc[]
* xref:VisualFrame.adoc[]
====

The angle to which the maximum angle in the data is mapped.   The corresponding “getter” function is `getEndAngle()`.

TIP: The  scale maximum set by  xref:LinearScaleSetMax.adoc[] or xref:TimeScaleSetMax.adoc[] also affects the displayed angle.

== Parameter
value::			angle in degrees



== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$VineShapeFrameSetEndAngle1.js[]
----
<1> {dataset}
<2> Create the `VineShapeFrame` object.
<3> Use `setEndAngle` to set a 90° angle.
<4> Use xref:GraphElementSetShapeFrame.adoc[] to add the `VineShapeFrame` to the xref:PointElement.adoc[].

image::commonscript:VineShapeFrameSetAngle.png[alt="point chart with VineShapeFrame end angle set to 90 degrees"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

