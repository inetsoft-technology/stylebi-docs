= GraphForm.setZIndex(value)
:navtitle: setZIndex()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GraphForm.setZIndex(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the layering order for xref:GraphForm.adoc[] objects. A shape with a larger `zIndex` overlays a shape with a smaller `zIndex`. To ensure that a manually drawn shape appears in front of other chart elements, select a large `zIndex`.  The corresponding “getter” function is `getZIndex()`.

include::partial$zIndexDefaults.adoc[]



== Parameter
value::			the index, a positive integer

== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GraphFormSetZIndex1.js[]
----
<1> {dataset}
<2> Create the  xref:GraphForm.adoc[] object.
<3> Use `setZIndex()` to move `form1` in front of `form2` by providing a higher `ZIndex`.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` objects to the Chart.

image::commonscript:GraphFormSetZIndex.png[alt="chart with overlapping forms ordered by Z-index"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

