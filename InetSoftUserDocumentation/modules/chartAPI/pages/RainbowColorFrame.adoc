= RainbowColorFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `RainbowColorFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:ColorFrame.adoc[]
====


The `RainbowColorFrame` object contains a continuous color frame that returns colors of the rainbow. To create a `RainbowColorFrame` object, call the `RainbowColorFrame` constructor.

[source,javascript]
var frame = new RainbowColorFrame('Quantity');


You can pass the name of a field (e.g., ‘Quantity’) to the constructor, or specify this later using the inherited xref:VisualFrameSetField.adoc[] property. You can also assign a `ColorFrame` using the xref:dashboardscript:ChartBindingInfoColorframe.adoc[] property in *onRefresh* script.




[#Example1]
== Example 1


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ColorFrame.adoc[].

. Bind a xref:dashboard:BarChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
Chart1.bindingInfo.setColorField("Total",Chart.NUMBER);
Chart1.bindingInfo.colorFrame = new RainbowColorFrame;

----

+
image::commonscript:RainbowColorFrame.png[alt="bar chart with rainbow color frame onRefresh"]
+
include::commonscript:partial$bindingInfoNote.adoc[]


[#Example2]
== Example 2

.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"],["NJ",200],["NY",300],["PA",50],["CT",100]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var frame = new RainbowColorFrame(); //<2>
frame.setField("Quantity"); //<3>
elem.setColorFrame(frame); //<4>
graph.addElement(elem);
----
<1> {dataset}
<2> Create the `RainbowColorFrame` object.
<3> Use the inherited xref:VisualFrameSetField.adoc[] property to specify the field for the `RainbowColorFrame`.
<4> Use xref:GraphElementSetColorFrame.adoc[] to add the `ColorFrame` to the Chart.

image::commonscript:RainbowColorFrame2.png[alt="bar chart with rainbow color frame"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]
