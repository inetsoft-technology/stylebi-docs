= GradientColorFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GradientColorFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:ColorFrame.adoc[]
====


The `GradientColorFrame` object contains a continuous color frame that returns gradations between two colors. To create a `GradientColorFrame` object, call the `GradientColorFrame` constructor.

[source,javascript]
var frame = new GradientColorFrame();


You can pass the name of a field (e.g., ‘Quantity’) to the constructor, or specify this later using the inherited xref:VisualFrameSetField.adoc[] property. You can also assign a `ColorFrame` using the xref:dashboardscript:ChartBindingInfoColorframe.adoc[] property in *onRefresh* script.


`GradientColorFrame` provides the special xref:chartAPI:GradientColorFrameSetFromColor.adoc[] and xref:GradientColorFrameSetToColor.adoc[] methods.


[#Example1]
== Example 1


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ColorFrame.adoc[].

. Bind a xref:dashboard:BarChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
Chart1.bindingInfo.setColorField("Total",Chart.NUMBER);
Chart1.bindingInfo.colorFrame = new GradientColorFrame;
Chart1.bindingInfo.colorFrame.fromColor = 0x0000FF;
Chart1.bindingInfo.colorFrame.toColor = 0xFF00FF;

----

+
image::commonscript:GradientColorFrame.png[alt="chart with GradientColorFrame via bindingInfo"]
+
include::commonscript:partial$bindingInfoNote.adoc[]


[#Example2]
== Example 2

.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"],["NJ",200],["NY",300],["PA",50],["CT",100]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var frame = new GradientColorFrame(); //<2>
frame.setField("Quantity"); //<3>
elem.setColorFrame(frame); //<4>
graph.addElement(elem);
----
<1> {dataset}
<2> Create the xref:GradientColorFrame.adoc[] object.
<3> Use `setField` to specify the field for the `GradientColorFrame`.
<4> Use xref:GraphElementSetColorFrame.adoc[] to add the `ColorFrame` to the Chart.

image::commonscript:GradientColorFrame_1.png[alt="chart with GradientColorFrame applied to element"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]
