= DefaultDataSet
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `DefaultDataSet`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[WARNING,caption=See Also]
====
xref:chartAPI:BindDatatoChartinScript.adoc[] for various approaches to binding data to a Chart.
====


Creates a `dataset` from an array. Note that you can also create a `dataset` without using this function; see xref:chartAPI:Dataset.adoc[] for examples.






== Example

.Set the dataset
[source,javascript]
----
var arr = [["State","Quantity"], ["NJ",200], ["NY",300]];
dataset = new DefaultDataSet(arr);
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
graph.addElement(elem);
----

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



