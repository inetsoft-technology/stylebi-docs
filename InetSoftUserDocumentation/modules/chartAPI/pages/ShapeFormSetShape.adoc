= ShapeForm.setShape(shape)
:navtitle: setShape()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `ShapeForm.setShape(shape)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====


Specifies the type of shape as a xref:GShape.adoc[] or xref:SVGShape.adoc[].  The corresponding “getter” function is `getShape()`.



== Parameter
shape::			xref:GShape.adoc[] +
xref:SVGShape.adoc[] +
xref:GShapeImageShape.adoc[]



== Example 1

.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"], ["NJ", 200], ["NY", 300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var form = new ShapeForm(); //<2>
form.setValues(['NJ',100]);
form.setShape(GShape.FILLED_TRIANGLE); //<3>
form.setColor(java.awt.Color(0xff0000));
form.setSize(new java.awt.Dimension(50,50));
graph.addForm(form); //<4>
graph.addElement(elem);

----
<1> {dataset}
<2> Create the  xref:ShapeForm.adoc[] object.
<3> Use `setShape()` to assign a xref:GShape.adoc[].
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:ShapeformSetShape1.png[alt="bar chart with a red filled triangle GShape overlaid at NJ 100"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


== Example 2


.Chart Component Script
[source,javascript]
----


dataset = [["State","Quantity"], ["NJ",200], ["NY",300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var logo = getImage("https://www.inetsoft.com/images/home/logo.gif");
var shape = new GShape.ImageShape();
shape.setImage(logo);
var form = new ShapeForm(); //<2>
form.setValues(['NJ',100]);
form.setShape(shape); //<3>
form.setSize(new java.awt.Dimension(150,40));
graph.addForm(form); //<4>
graph.addElement(elem);
----
<1> {dataset}
<2> Create the  xref:ShapeForm.adoc[] object.
<3> Use `setShape()` to assign an `ImageShape`.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:ShapeformSetShape2.png[alt="bar chart with an ImageShape logo overlaid using ShapeForm at NJ 100"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

