= StackTextFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `StackTextFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:TextFrame.adoc[]
* xref:VisualFrame.adoc[]
====


The `StackTextFrame` object contains data for a  xref:dashboard:MultipleMeasureChart.adoc#Stack[Stacked Chart]. To create a `StackTextFrame` object, call the `StackTextFrame` constructor.

[source, javascript]
var frame = new StackTextFrame (elem);

To stylize or reposition text created using a `StackTextFrame`, use the xref:GraphElementSetTextSpec.adoc[] and xref:GraphElementSetLabelPlacement.adoc[] fucntions.

== Parameter
elem::			a xref:GraphElement.adoc[] object






== Example

.Chart Component Script
[source,javascript]
----
var elem = graph.getElement(0); //<1>
var frame = new StackTextFrame(elem); //<2>
elem.setTextFrame(frame);  //<3>
----

<1> Use xref:EGraphGetElement.adoc[] to get a handle to the xref:GraphElement.adoc[].
<2> Create the `StackTextFrame` object.
<3> Use xref:GraphElementSetTextFrame.adoc[] to add the `TextFrame` to the element.

image::commonscript:StackTextFrame1.png[alt="stacked bar chart with total labels added via StackTextFrame"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


