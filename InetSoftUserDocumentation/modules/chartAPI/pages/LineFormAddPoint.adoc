= LineForm.addPoint(value)
:navtitle: addPoint()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LineForm.addPoint(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====


Specifies the pixel location (integer values) or proportional location (fractional values) defining a point on the line. (Positive values specify distance from left/bottom. Negative values specify distance from right/top.)






== Parameter
value::			a subclass of {javaawtgeomPoint2D} +
(e.g., {javaawtPoint} for pixels, {javaawtgeomPoint2DDouble} for proportion)


== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LineFormAddPoint1.js[]
----
<1> {dataset}
<2> Create the  xref:LineForm.adoc[] object.
<3> Use `addPoint()` to set `form1` points in pixels.
<4> Use `addPoint()` to set `form2` points proportionally.
<5> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:GraphFormSetPoint.png[alt="bar chart with line forms at pixel and proportional points"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

