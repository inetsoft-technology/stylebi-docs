= TimeScale.setIncrement(value)
:navtitle: setIncrement()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `TimeScale.setIncrement(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:Scale.adoc[]
====

Specifies an integer increment at which to place labels on the xref:TimeScale.adoc[] axis. The integer value designates the increment in terms of the prevailing time unit, e.g., Weeks, Months, Quarters, etc.  The corresponding “getter” function is `getIncrement()`.

== Parameter
value::			an integer





== Example

.Chart Component Script
[source,javascript]
----
graph.getScale("Order Date").setIncrement(12) <1>

----

<1> Set increment to 12 weeks.


image::commonscript:TimeScaleSetIncrement.png[alt="line chart with Order Date axis tick marks set every 12 weeks"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

