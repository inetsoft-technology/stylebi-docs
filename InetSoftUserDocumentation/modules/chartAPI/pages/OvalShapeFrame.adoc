= OvalShapeFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `OvalShapeFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:MultiShapeFrame.adoc[]
* xref:VisualFrame.adoc[]
====

The `OvalShapeFrame` object contains the shape styles for oval elements of varying aspect ratio. To create a `OvalShapeFrame` object, call the `OvalShapeFrame` constructor.

[source,javascript]
var frame = new OvalShapeFrame('Total');



You can pass the name of a field (e.g., ‘Total’) to the constructor, or specify this later using the inherited xref:VisualFrameSetField.adoc[] property. You can also set this using the xref:dashboardscript:ChartBindingInfoShapeFrame.adoc[]  syntax in *onRefresh* script . See xref:#Example2[] below.

`OvalShapeFrame` provides the special xref:OvalShapeFrameSetFill.adoc[] method.






== Example 1

.Chart Component Script
[source,javascript]
----
dataset = [["State","Quantity","Total"], ["NJ",200,30], ["NY",300,15],["PA",150,5]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
var frame = new OvalShapeFrame("Total"); //<2>
elem.setShapeFrame(frame); //<3>
graph.addElement(elem);

----
<1> {dataset}
<2> Create the `OvalShapeFrame` object.
<3> Use xref:GraphElementSetShapeFrame.adoc[] to add the `OvalShapeFrame` to the xref:IntervalElement.adoc[].

image::commonscript:OvalShapeFramme2.png[alt="bar chart with oval shapes of varying aspect ratio"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[#Example2]
== Example 2

This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ShapeFrame.adoc[].

. Bind a xref:dashboard:BarChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)
+
.onRefresh Script
[source,javascript]
----
Chart1.bindingInfo.setShapeField("Total",Chart.NUMBER);
Chart1.bindingInfo.shapeFrame = new OvalShapeFrame();

----

+
image::commonscript:OvalShapeFrame.png[alt="bar chart with oval shape frame applied"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

