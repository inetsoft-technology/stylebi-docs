= ParallelCoord.setScales(scales)
:navtitle: setScales()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `ParallelCoord.setScales(scales)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:Coordinate.adoc[]
====

Specifies the set of parallel scales to use. These can also be specified as arguments to the constructor.

== Parameter
scales::			Array of xref:Scale.adoc[] objects




== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$ParallelCoordSetScales1.js[]
----
<1> {dataset}
<2> Create the xref:LinearScale.adoc[] objects.
<3> Create the `ParallelCoord` from the `LinearScale` objects.
<4> Use xref:EGraphSetCoordinate.adoc[] to add the `Coordinate` to the Chart.

image::commonscript:ParallelCoordssetCoords.png[alt="parallel coordinates chart with scales set via array"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

