= AxisSpec.setAllTicks(Boolean)
:navtitle: setAllTicks()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

Specifies whether *tick marks* are displayed at positions where duplicate labels have been suppressed on a xref:TimeScale.adoc[]. When a date format produces the same text for multiple consecutive ticks (for example, formatting monthly data as `'yyyy'` causes every month in the same year to display the same label), the duplicate labels are hidden. This property controls whether tick marks are still drawn at those hidden-label positions.  {noGetter}

== Parameter
Boolean:: `true`: show tick marks at all positions, including those with suppressed duplicate labels +
`false`: hide tick marks where duplicate labels have been suppressed

== Example

.Chart Component Script
[source,javascript]
--
include::scriptexample:partial$AxisSpecSetAllTicks1.js[]
--
<1> {dataset} Twenty-four monthly data points spanning two years. Formatting as `'yyyy'` means all 12 months of each year share the same label — 11 out of 12 per year are suppressed as duplicates.
<2> Create the `AxisSpec` object. Formatting as `'yyyy'` causes duplicate labels; the red line color makes tick marks easy to spot.
<3> Use `setAllTicks(true)` to keep a tick mark at every month position even where the duplicate label is hidden. Change to `false` to see only one tick per year.

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use xref:chartAPI:EGraphGetCoordinate.adoc[] ①, xref:chartAPI:RectCoordGetXScale.adoc[] ②, and xref:chartAPI:ScaleGetAxisSpec.adoc[] ③ to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object, xref:chartAPI:Scale.adoc[] object, and so on. For example:

[source,javascript]
--
include::scriptexample:partial$AxisSpecSetAllTicks2.js[]
--
=====
