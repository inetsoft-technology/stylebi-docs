= GShape.setFillColor(color)
:navtitle: setFillColor()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GShape.setFillColor(color)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Sets the fill color for a xref:GShape.adoc[] object.  The corresponding “getter” function is `getFillColor()`.



== Parameters
color:: {javaawtcolor}		 to fill the shape







== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GShapeSetFillColor1.js[]
----
<1> {dataset}
<2> Use `create()` to create the shape.
<3> Use `setFillColor()` to assign the color.
<4> Pass the `shape` to the xref:StaticShapeFrame.adoc[] constructor.
<5> Use xref:GraphElementSetShapeFrame.adoc[] to add the `ShapeFrame` to the xref:IntervalElement.adoc[].
+
image::commonscript:GShapeImageShape4.png[alt="point chart with circle shape fill color set to red"]
+
include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



