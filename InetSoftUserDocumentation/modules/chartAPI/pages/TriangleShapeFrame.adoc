= TriangleShapeFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `TriangleShapeFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====

The `TriangleShapeFrame` object contains the shape styles for isosceles trapezoid elements with varying width ratios. To create a `TriangleShapeFrame` object, call the `TriangleShapeFrame` constructor.

[source,javascript]
var frame = new TriangleShapeFrame('Total');

You can pass the name of a field (e.g., ‘Total’) to the constructor, or specify this later using the inherited xref:VisualFrameSetField.adoc[] property. You can also assign a `ShapeFrame` using the `bindingInfo.shapeFrame` property in *onRefresh* script. See xref:#Example2[] below.

`TriangleShapeFrame` provides the special xref:TriangleShapeFrameSetFill.adoc[] method.



== Example 1

.Chart Component Script
[source,javascript]
----
dataset = [["State","Quantity","Total"], ["NJ",200,30],["NY",300,15], ["PA",150,5]]; //<1>
graph = new EGraph();
var elem = new PointElement("State", "Quantity");
var sizeframe = new StaticSizeFrame();
sizeframe.setSize(10);
var frame = new TriangleShapeFrame("Total"); //<2>
elem.setShapeFrame(frame); //<3>
elem.setSizeFrame(sizeframe);
graph.addElement(elem);
----
<1> {dataset}
<2> Create the `TriangleShapeFrame` object.
<3> Use xref:GraphElementSetShapeFrame.adoc[] to add the `TriangleShapeFrame` to the xref:PointElement.adoc[].

image::commonscript:TriangleShapeFrame2.png[alt="point chart with trapezoid shapes sized proportionally via TriangleShapeFrame Total"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]




[#Example2]
== Example 2


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ShapeFrame.adoc[].

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
Chart1.bindingInfo.setShapeField("Total",Chart.NUMBER);
Chart1.bindingInfo.shapeFrame = new TriangleShapeFrame;

----

+
image::commonscript:TriangleShapeFrame.png[alt="point chart with TriangleShapeFrame applied via bindingInfo shapeFrame property"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

