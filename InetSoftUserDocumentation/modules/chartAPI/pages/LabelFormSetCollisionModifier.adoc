= LabelForm.setCollisionModifier(value)
:navtitle: setCollisionModifier()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LabelForm.setCollisionModifier(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====


Specifies how collisions (labels occupying the same location) should be handled.  The corresponding “getter” function is `getCollisionModifier()`.



== Parameter
value::			One of the following constants:
+
[source]
VLabel.MOVE_NONE  // no adjustment
VLabel.MOVE_FREE  // move label any direction
VLabel.MOVE_RIGHT // move label right
VLabel.MOVE_UP    // move label up

== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LabelFormSetCollisionModifier1.js[]
----
<1> {dataset}
<2> Create the  xref:LabelForm.adoc[] object.
<3> Use `setCollisionModifier()` to allow free label movement.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:LabelFormSetCollision.png[alt="chart with collision modifier moving overlapping labels"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

