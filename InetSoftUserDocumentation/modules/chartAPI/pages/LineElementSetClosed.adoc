= LineElement.setClosed(Boolean)
:navtitle: setClosed()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LineElement.setClosed(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphElement.adoc[]
====


Specifies whether the line object should be automatically closed (i.e., endpoints connected). {noGetter}







== Parameters
Boolean:: `true`: close the figure +
`false`:  do not close





== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LineElementSetClosed1.js[]
----

<1> {dataset}
<2> Create the `LineElement` object.
<3> Use `setClosed` to close the figure.
<4> Use xref:EGraphAddElement.adoc[] to add the `LineElement` to the Chart.


image::commonscript:LineElementsetClosed.png[alt="line chart with endpoints closed into polygon"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:EGraphGetElement.adoc[] {circle1} to obtain a handle to the desired xref:GraphElement.adoc[] object. For example:

[source,javascript]
----
var elem = graph.getElement(0); //<1>
elem.setClosed(true);
----
=====





