= GShape.ImageShape
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GShape.ImageShape`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

The `GShape.ImageShape` object contains a custom image to be used as a fill pattern with xref:StaticShapeFrame.adoc[] and xref:CategoricalShapeFrame.adoc[] objects. To create an `ImageShape` object in element script , call the object constructor:

[source,javascript]
var shape = new GShape.ImageShape();

You can provide the image location as input to the constructor, for example:

[source,javascript]
var shape = new GShape.ImageShape("https://.../image.gif");

You can also specify this later using xref:GShapeImageShapeSetImage.adoc[] method in *Component* script, or using the `GShape.ImageShape.image` property in *onRefresh* script. See xref:#Example3[] below.

`GShape.ImageShape` provides the following methods:
[square]
include::ROOT:partial$MINITOC_GShapeImageShape.adoc[]



== Example 1

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GShapeImageShape1.js[]
----
<1> {dataset}
<2> Create the `GShape.ImageShape` shape.
<3> Use xref:GShapeImageShapeSetImage.adoc[] to assign the desired image to the `shape`.
<4> Pass the `shape` to   the xref:StaticShapeFrame.adoc[] constructor.
<5> Use xref:GraphElementSetShapeFrame.adoc[] to add the `ShapeFrame` to the xref:IntervalElement.adoc[].

image::commonscript:GShapeImageShape2.png[alt="bar chart with custom image shape as fill pattern"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


== Example 2

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GShapeImageShape2.js[]
----
<1> {dataset}
<2> Pass the image to the `GShape.ImageShape()` constructor to create the `shape`.
<3> Use xref:CategoricalShapeFrameSetShape.adoc[] to assign the  `shape` to the data.
<4> Use xref:GraphElementSetShapeFrame.adoc[] to add the `ShapeFrame` to the xref:IntervalElement.adoc[].

image::commonscript:GShapeImageShape3.png[alt="bar chart with image shape assigned per data category"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]




[#Example3]
== Example 3


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ShapeFrame.adoc[].

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
include::scriptexample:partial$GShapeImageShape3.js[]
----
+
image::commonscript:GShapeImageShape.png[alt="point chart with image shape via bindingInfo in onRefresh script"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

