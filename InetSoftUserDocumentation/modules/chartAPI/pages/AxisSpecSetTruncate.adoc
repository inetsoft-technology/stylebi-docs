= AxisSpec.setTruncate(Boolean)
:navtitle: setTruncate()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `AxisSpec.setTruncate(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies whether axis labels are truncated with [`..`] when there is insufficient space to display the full text. When `false`, crowded labels may instead be hidden, overlapped, or rotated. {noGetter}

== Parameter
Boolean:: `true`: truncate labels that do not fit (default) +
`false`: do not truncate labels (crowded labels may be hidden or rotated)

== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$AxisSpecSetTruncate1.js[]
----
<1> {dataset} Five long product names are used to ensure labels exceed the available width per category.
<2> Create the `AxisSpec` object.
<3> Use `setTruncate(true)` to truncate labels that do not fit.

image::commonscript:AxisSpecSetTruncate.png[alt="bar chart with truncated axis labels"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use xref:chartAPI:EGraphGetCoordinate.adoc[] ①, xref:chartAPI:RectCoordGetXScale.adoc[] ②, and xref:chartAPI:ScaleGetAxisSpec.adoc[] ③ to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object, xref:chartAPI:Scale.adoc[] object, and so on. For example:

[source,javascript]
----
include::scriptexample:partial$AxisSpecSetTruncate2.js[]
----
=====
