= Coordinate.rotate(value)
:navtitle: rotate()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `Coordinate.rotate(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Rotate the axes by the specified angle.

== Parameter
value::			an angle in degrees


== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$CoordinateRotate1.js[]
----
<1> {dataset}
<2> Create the xref:Coordinate.adoc[] object.
<3> Use `rotate` to rotate the polar coordinates 45°.
<4> Use xref:EGraphSetCoordinate.adoc[] to add the `Coordinate` to the Chart.

image::commonscript:Coordinaterotate.png[alt="polar coordinate chart with axes rotated 45 degrees"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====


To change the property on a Chart that was *previously created* with the Chart Editor, use  xref:chartAPI:EGraphGetCoordinate.adoc[] ① to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object. For example:

[source,javascript]
----
var coord = graph.getCoordinate(); //<1>
var polar = new PolarCoord(coord);
polar.setType(PolarCoord.PLUS);
polar.rotate(90);
polar.setHoleRatio(.25);
graph.setCoordinate(polar);

----
=====