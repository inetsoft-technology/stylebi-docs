= LinearScale.setMin(value)
:navtitle: setMin()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LinearScale.setMin(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:Scale.adoc[]
====

Specifies the minimum value of the xref:LinearScale.adoc[].  The corresponding “getter” function is `getMin()`.




== Parameter
value::			Number specifying the minimum scale value






== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LinearScaleSetMin1.js[]
----

<1> {dataset}
<2> Create the xref:LinearScale.adoc[] object.
<3> Use `setMin` to set the  desired minimum.
<4> Use `setMax` to set the  desired maximum.
<5> Use xref:EGraphSetScale.adoc[] to assign the `Scale` to the Chart.

image::commonscript:LinearScaleSetMinMax.png[alt="bar chart with custom min and max scale"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]




[TIP]
=====


To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:chartAPI:EGraphGetCoordinate.adoc[] ① and xref:chartAPI:RectCoordGetYScale.adoc[] ② to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] and xref:chartAPI:Scale.adoc[]. For example:

[source,javascript]
----
var coord = graph.getCoordinate(); //<1>
var scale = coord.getYScale(); //<2>
// Compact Syntax: var scale = graph.getCoordinate().getYScale();

scale.setMin(2000000);
scale.setMax(15000000);
----
=====


