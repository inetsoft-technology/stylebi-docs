= StaticTextureFrame.setTexture(value)
:navtitle: setTexture()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `StaticTextureFrame.setTexture(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====

Specifies the static texture for graphical elements in element script . If the data in the column assigned by the inherited xref:VisualFrameSetField.adoc[] function are `GTexture` numbers, these data values are used instead of the `setTexture()` setting. You can also set this using the `bindingInfo.textureFrame.texture`  property syntax in *onRefresh* script . See xref:#Example2[] below.  The corresponding “getter” function is `getTexture()`.


== Parameter
value::
include::commonscript:partial$gTexture.adoc[]















== Example 1

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$StaticTextureFrameSetTexture1.js[]
----
<1> {dataset}
<2> Create the `StaticTextureFrame` object.
<3> Use `setTexture` to assign `GTexture.PATTERN_18`.
<4> Use xref:GraphElementSetTextureFrame.adoc[] to add the `TextureFrame` to the xref:IntervalElement.adoc[].

image::commonscript:StaticTextureFrame2setTexture.png[alt="bar chart with GTexture.PATTERN_18 applied via StaticTextureFrame setTexture"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====

To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:EGraphGetElement.adoc[] {circle1} and xref:GraphElementGetTextureFrame.adoc[] {circle2} to obtain a handle to the desired xref:GraphElement.adoc[] and xref:TextureFrame.adoc[]  objects. For example:

[source,javascript]
----
var elem = graph.getElement(0); //<1>
var frame = elem.getTextureFrame(); //<2>
frame.setTexture(GTexture.PATTERN_18);
----
=====



[#Example2]
== Example 2


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:TextureFrame.adoc[].

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
include::scriptexample:partial$StaticTextureFrameSetTexture2.js[]
----

+
image::commonscript:StaticTextureFrame_2.png[alt="point chart with GTexture.PATTERN_5 set via bindingInfo textureFrame texture"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

