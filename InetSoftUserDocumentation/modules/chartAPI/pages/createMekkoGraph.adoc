= createMekkoGraph(xdim, innerDim, measure, colorDim, textCols)
:navtitle: createMekkoGraph()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `createMekkoGraph(xdim, innerDim, measure, colorDim, textCols)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

The `createMekkoGraph()` function generates a xref:dashboard:MarimekkoChart.adoc[].

== Parameter
xdim::	The dimension to place on the X-axis.
innerDim::	The dimension used to break out each X column.
measure::	The measure (with aggregation method) to represent on the chart.
colorDim::	 		Name of dimension column whose values should be represented by color.
textCols::	 Array of dimension column names from which to render labels.









== Example


.Chart Component Script
[source,javascript]
----
dataset = viewsheet['Query1']; //<1>
graph = createMekkoGraph("state", "city", "price", "state"); //<2>
----
<1> See xref:Dataset.adoc[] for more information about setting the `dataset`.
<2> Create the Mekko chart.
+
image::dashboard:mekko1.png[alt="marimekko chart with state on X-axis city breakout colored by state"]
+
include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


