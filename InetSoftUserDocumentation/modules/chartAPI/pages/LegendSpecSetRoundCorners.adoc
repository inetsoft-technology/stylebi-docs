= LegendSpec.setRoundCorners(Boolean)
:navtitle: setRoundCorners()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LegendSpec.setRoundCorners(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies whether the legend`'s border has rounded corners. This is distinct from xref:LegendSpecSetSymbolRoundCorners.adoc[], which rounds the corners of the legend`'s symbols rather than its border. The corresponding “getter” function is `getRoundCorners()`.

== Parameter
Boolean::			`true`: round the legend border corners +
`false`: use square legend border corners




== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LegendSpecSetRoundCorners1.js[]
----
<1> {dataset}
<2> Create the xref:LegendSpec.adoc[] object.
<3> Use `setRoundCorners` to round the corners of the `LegendSpec` border.
<4> Use xref:VisualFrameSetLegendSpec.adoc[] to assign the `LegendSpec` to the `ColorFrame`.

image::commonscript:LegendSpecsetRoundCorners.png[alt="legend with rounded border corners applied"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====

To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:chartAPI:EGraphGetElement.adoc[] {circle1}, xref:chartAPI:GraphElementGetColorFrame.adoc[] {circle2}, and xref:chartAPI:VisualFrameGetLegendSpec.adoc[] {circle3} to obtain a handle to the desired xref:chartAPI:GraphElement.adoc[],  xref:chartAPI:ColorFrame.adoc[], and xref:chartAPI:LegendSpec.adoc[]. For example:

[source,javascript]
----
var elem = graph.getElement(0); // <1>
var frame = elem.getColorFrame(); // <2>
var spec = frame.getLegendSpec(); // <3>
// Compact syntax: var spec = graph.getElement(0).getColorFrame().getLegendSpec();

spec.setRoundCorners(true);

----
=====
