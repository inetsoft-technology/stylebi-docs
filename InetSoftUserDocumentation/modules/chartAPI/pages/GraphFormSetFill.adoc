= GraphForm.setFill(Boolean)
:navtitle: setFill()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GraphForm.setFill(Boolean)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies whether the form should be filled or unfilled. {noGetter}


== Parameter
Boolean:: `true`: fill the shape +
`false`: do not fill the shape




== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GraphFormSetFill1.js[]
----
<1> {dataset}
<2> Create the  xref:GraphForm.adoc[] object.
<3> Use `setFill()` to fill the `GraphForm`.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:GraphFormSetFill_1.png[alt="chart with filled line form shape"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

