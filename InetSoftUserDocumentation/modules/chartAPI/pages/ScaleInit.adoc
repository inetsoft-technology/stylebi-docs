= Scale.init(dataset)
:navtitle: init()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `Scale.init(dataset)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Forces the immediate computation of the automatic scale attributes. The `init()` method is only required if you need to access automatically-computed xref:Scale.adoc[] attributes within your Chart script. These attributes are not available within a script until the scale is initialized via the `init()` method.

== Parameter
dataset::			The Chart xref:Dataset.adoc[] attribute



== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$ScaleInit1.js[]
----

<1> {dataset}
<2> Create   the xref:Scale.adoc[] object.
<3> Use `init` to compute the xref:Scale.adoc[] attributes for `dataset`.
<4> Use xref:EGraphSetScale.adoc[] to assign the `Scale` to the Chart.


include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


