= RectForm
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `RectForm`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====


The `RectForm` object contains information for rectangles manually drawn on the chart. To create a `RectForm` object, call the `RectForm` constructor.

[source, javascript]
var rect = new RectForm();

`RectForm` provides the following methods:
[square]
include::ROOT:partial$MINITOC_RectForm.adoc[]