= EGraph.setX2TitleSpec(spec)
:navtitle: setX2TitleSpec()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `EGraph.setX2TitleSpec(spec)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the X-axis title information for X-axis *above* the chart.  The corresponding “getter” function is xref:chartAPI:EGraphGetX2TitleSpec.adoc[].

== Parameter
spec::			A xref:chartAPI:TitleSpec.adoc[] object







== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$EGraphSetX2TitleSpec1.js[]
----
<1> {dataset}
<2> Create the xref:TitleSpec.adoc[] object.
<3> Use `setX2TitleSpec()` to add the `TitleSpec` to Chart.


image::commonscript:EGraphaddsetX2TitleSpec.png[alt="EGraph with X2 title spec set"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]



[TIP]
=====

To change the property on a Chart that was previously created with the Chart Editor, use xref:chartAPI:EGraphGetX2TitleSpec.adoc[] ① to get a handle to the `TitleSpec` object. For example:


[source,javascript]
----
var spec = graph.getX2TitleSpec(); <1>
spec.setLabel("X Title");

----
=====



