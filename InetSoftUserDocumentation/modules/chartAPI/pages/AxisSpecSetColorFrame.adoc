= AxisSpec.setColorFrame(frame)
:navtitle: setColorFrame()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `AxisSpec.setColorFrame(frame)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies a mapping between axis label values and colors. The corresponding "getter" function is `getColorFrame()`.

== Parameter
frame:: A xref:ColorFrame.adoc[] object

== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$AxisSpecSetColorFrame1.js[]
----
<1> {dataset}
<2> Create the `AxisSpec` object.
<3> Create the xref:CategoricalColorFrame.adoc[] object and use xref:CategoricalColorFrameSetColor.adoc[] to map values to colors.
<4> Use `setColorFrame` to assign the `ColorFrame` to the axis.

image::commonscript:AxisSpecsetColorFrame.png[alt="bar chart with colored axis labels"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====
To change the property on a Chart that was *previously created* with the Chart Editor, use xref:chartAPI:EGraphGetCoordinate.adoc[] ①, xref:chartAPI:RectCoordGetXScale.adoc[] ②, and xref:chartAPI:ScaleGetAxisSpec.adoc[] ③ to obtain a handle to the desired xref:chartAPI:Coordinate.adoc[] object, xref:chartAPI:Scale.adoc[] object, and so on. For example:

[source,javascript]
----
include::scriptexample:partial$AxisSpecSetColorFrame2.js[]
----
=====
