= DataSet.setOrder(dim, arr)
:navtitle: setOrder()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `DataSet.setOrder(dim, arr)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[WARNING,caption=See Also]
====
xref:chartAPI:DataSetGetData.adoc[] to retrieve data from the DataSet object.
====



Set a manual label ordering for a given dimension field.

== Parameters
dim::		Name of the dimension to sort
arr::  Array of strings with desired label order:
+
[source,javascript]
----
['label1','label2','label3',...]
----








== Example


.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"],["NJ", 200],["NY", 300],["PA", 25]];
dataset.setOrder('State',['PA','NY','NJ']); <1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
graph.addElement(elem);
----
<1> Sort the 'State' field in the specified order.

image::commonscript:datasetsetOrder.png[alt="bar chart with dataset sorted by custom order"]



include::chartAPI:partial$scriptThatModifiesGraph.adoc[]