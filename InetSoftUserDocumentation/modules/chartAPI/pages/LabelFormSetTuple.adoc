= LabelForm.setTuple(value)
:navtitle: setTuple()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `LabelForm.setTuple(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:GraphForm.adoc[]
====


Specifies the point in logical space for the label text. The coordinates of the point are relative to the prevailing axis scaling.  The corresponding “getter” function is `getTuple()`.





== Parameter
value::			An [X,Y] pair



== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$LabelFormSetTuple1.js[]
----
<1> {dataset}
<2> Create the  xref:LabelForm.adoc[] object.
<3> Use `setTuple()` to set the axis location for the label.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:LabelFormSetTuple.png[alt="chart with label positioned using tuple axis coordinates"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

