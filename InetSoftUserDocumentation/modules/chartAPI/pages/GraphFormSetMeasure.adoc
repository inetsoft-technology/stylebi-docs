= GraphForm.setMeasure(col)
:navtitle: setMeasure()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `GraphForm.setMeasure(col)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Specifies the measure for which the form should be displayed. If the Chart contains a measure of this name, the xref:GraphForm.adoc[] object is displayed. Otherwise, it is not displayed. This is useful for xref:FacetCoord.adoc[] Charts, when the `GraphForm` should be displayed only for the Chart that represents a particular measure.  The corresponding “getter” function is `getMeasure()`.







== Parameter
col::			name of a column (String)

== Example


.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$GraphFormSetMeasure1.js[]
----
<1> {dataset}
<2> Create the  xref:GraphForm.adoc[] object.
<3> Use `setMeasure()` to display the `GraphForm` only for the '`Sales`' measure.
<4> Use xref:EGraphAddForm.adoc[] to add the `GraphForm` to the Chart.

image::commonscript:GraphFormSetMeasure.png[alt="facet chart with label form on specific measure"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

