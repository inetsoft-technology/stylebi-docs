= TextureFrame
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `TextureFrame`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=created by]
====
[square]
* xref:LeftTiltTextureFrame.adoc[]
* xref:OrientationTextureFrame.adoc[]
* xref:RightTiltTextureFrame.adoc[]
* xref:GridTextureFrame.adoc[]
* xref:CategoricalTextureFrame.adoc[]
* xref:StaticTextureFrame.adoc[]
====


[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====


The `TextureFrame` object contains the texture for visual chart objects. You can use a `TextureFrame` object to represent data dimensions with texture (texture coding), or to apply a fixed (static) texture pattern. To create a `TextureFrame`, use constructors such as xref:GridTextureFrame.adoc[] or xref:StaticTextureFrame.adoc[].











== Example 1

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script to the Chart component. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddComponentScript[Add Component Script].)
+
.Chart Component Script
[source,javascript]
----
var elem = graph.getElement(0); //<1>
var frame = new StaticTextureFrame(); //<2>
frame.setTexture(GTexture.PATTERN_5);
elem.setTextureFrame(frame); //<3>
----
<1> Use xref:EGraphGetElement.adoc[] to get a handle to the xref:PointElement.adoc[].
<2> Create the `StaticTextureFrame` object.
<3> Use xref:GraphElementSetTextureFrame.adoc[] to add the `StaticTextureFrame` to the xref:PointElement.adoc[].

+
image::commonscript:StaticTextureFrame.png[alt="point chart with StaticTextureFrame PATTERN_5 applied to all elements"]
+
include::chartAPI:partial$scriptThatModifiesGraph.adoc[]




[#Example2]
== Example 2


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:TextureFrame.adoc[].

. Bind a xref:dashboard:BarChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
Chart1.bindingInfo.setShapeField("Total",Chart.NUMBER);
Chart1.bindingInfo.textureFrame = new LeftTiltTextureFrame;
----

+
image::commonscript:LeftTiltTextureFrame.png[alt="bar chart with LeftTiltTextureFrame applied via bindingInfo textureFrame property"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

