= EGraph.getFormCount()
:navtitle: getFormCount()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `EGraph.getFormCount()`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

Returns the number of forms currently present on the chart.






== Returns
Integer::			Number of form objects






.Chart Component Script
[source,javascript]
----
dataset = [["State", "Quantity"], ["NJ", 200], ["NY", 300]]; //<1>
graph = new EGraph();
var elem = new IntervalElement("State", "Quantity");
graph.addForm(new LineForm()); //<2>
graph.addForm(new LineForm()); //<2>
graph.addForm(new LineForm()); //<2>
var formCount = graph.getFormCount(); //<3>
for (var i=0; i<formCount; i++) {
  graph.getForm(i).addValues(['NJ',i*100]);
  graph.getForm(i).addValues(['NY',300]);
  graph.getForm(i).setColor(java.awt.Color(0xff0000));
}
graph.addElement(elem);
----
<1> {dataset}
<2> Add xref:GraphForm.adoc[] objects to Chart.
<3> Use `getFormCount` to return the number of `GraphForms`.

image::commonscript:EGraphgetFormCount.png[alt="EGraph with form count retrieved"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

