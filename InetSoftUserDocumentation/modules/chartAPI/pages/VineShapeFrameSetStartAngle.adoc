= VineShapeFrame.setStartAngle(value)
:navtitle: setStartAngle()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `VineShapeFrame.setStartAngle(value)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:MultiShapeFrame.adoc[]
* xref:VisualFrame.adoc[]
====

The angle to which the minimum angle in the data is mapped.   The corresponding “getter” function is `getStartAngle()`.


TIP: The  scale minimum set by  xref:LinearScaleSetMin.adoc[] or xref:TimeScaleSetMin.adoc[] also affects the displayed angle.

== Parameter
value::			angle in degrees



== Example

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$VineShapeFrameSetStartAngle1.js[]
----
<1> {dataset}
<2> Create the `VineShapeFrame` object.
<3> Use `setStartAngle` to set a 0° angle.
<4> Use xref:GraphElementSetShapeFrame.adoc[] to add the `VineShapeFrame` to the xref:PointElement.adoc[].

image::commonscript:VineShapeFrameSetAngle_1.png[alt="point chart with VineShapeFrame start angle set to 0 degrees"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]

