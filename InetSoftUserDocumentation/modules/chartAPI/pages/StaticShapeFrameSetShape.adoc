= StaticShapeFrame.setShape(shape)
:navtitle: setShape()
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Chart Script API reference for `StaticShapeFrame.setShape(shape)`. This is part of StyleBI's Chart Script (Chart API) scripting layer — a last-resort approach for chart customization/interactivity. See chartAPI:ChartScriptTutorial.adoc for guidance on when to use scripting vs. UI-driven alternatives (built-in toolbar tools, Ad Hoc Editor, Form-driven dynamic properties).
endif::ai[]

[CAUTION,caption=inherits from]
====
[square]
* xref:VisualFrame.adoc[]
====

Specifies the static shape for graphical elements. If the data in the column assigned to the inherited xref:VisualFrameSetField.adoc[] property are shape names, these data values are used instead of `setShape()` setting. You can also set this using the `bindingInfo.shapeFrame.setShape()`  syntax in *onRefresh* script . See xref:Example2[] below.  The corresponding “getter” function is `getShape()`.

== Parameter
shape:: xref:GShape.adoc[], `GShape.ImageShape`, or xref:SVGShape.adoc[] object



== Example 1

.Chart Component Script
[source,javascript]
----
include::scriptexample:partial$StaticShapeFrameSetShape1.js[]
----
<1> {dataset}
<2> Create the `StaticShapeFrame` object.
<3> Use `setShape` to assign a cross-shape to the xref:PointElement.adoc[].
<4> Use xref:GraphElementSetShapeFrame.adoc[] to add the `StaticShapeFrame` to the xref:PointElement.adoc[].

image::commonscript:StaticShapeFrame2setShape.png[alt="point chart with cross shape applied to all elements via setShape"]

include::chartAPI:partial$scriptThatModifiesGraph.adoc[]


[TIP]
=====

To change the property on a Chart that was *previously created* with the Chart Editor, use “getter” methods such as xref:EGraphGetElement.adoc[] {circle1} and xref:GraphElementGetShapeFrame.adoc[] {circle2} to obtain a handle to the desired xref:GraphElement.adoc[] and xref:ShapeFrame.adoc[]  objects. For example:

[source,javascript]
----
var elem = graph.getElement(0); //<1>
var frame = elem.getShapeFrame();
frame.setShape(GShape.CROSS);
----
=====




[#Example2]
== Example 2


This example illustrates how to use xref:dashboardscript:ChartBindingInfo.adoc[] properties to control the xref:ShapeFrame.adoc[].

. Bind a xref:dashboard:PointChart.adoc[]  to the sample ‘All Sales’ Data Worksheet, with ‘Company’ (top 5) on the X-axis, and Sum(Total) on the Y-axis.
+
include::ROOT:partial$AllSalesQuery.adoc[]


. Add the following script in the *onRefresh* handler. (See xref:dashboardscript:AddScriptToDashboard.adoc#AddDashboardScript[Add Dashboard Script].)

+
.onRefresh Script
[source,javascript]
----
include::scriptexample:partial$StaticShapeFrameSetShape2.js[]
----

+
image::commonscript:StaticShapeFrame_1.png[alt="point chart with arrowbar shape set via bindingInfo shapeFrame setShape"]
+
include::commonscript:partial$bindingInfoNote.adoc[]

