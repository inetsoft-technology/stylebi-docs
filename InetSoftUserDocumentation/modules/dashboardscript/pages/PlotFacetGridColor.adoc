= facetGridColor
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: Sets the color for the global border of a facet-type chart (a chart with multiple dimensions on the axes).
endif::ai[]


Sets the color for the global border of a facet-type chart (a chart with multiple dimensions on the axes).

== Type
include::partial$colorSyntax.adoc[]

== Example
[source,javascript]
Chart1.facetGrid = true;
Chart1.facetGridColor = 'red';

image::commonscript:facetGrid.png[alt="chart with facet grid line color set"]




[TIP]
====
Either syntax below can be used in xref:dashboardscript:AddScriptToDashboard.adoc#AddComponentScript[component script]:
[source,javascript]
facetGridColor          // unqualified
Chart1.facetGridColor   // qualified

{secondForm}
====