= Legend Properties
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]


// metadata for AI
ifdef::ai[]
Reference for Chart Legend properties (also called chart legend color, legend position, or legend settings), accessed via right-click on the legend. Covers the General tab (position, legend border style/color, title text, title visibility, ignore null, and symbol size), Scale tab (logarithmic scale, reverse orientation, and include zero for measure/numerical legends), Alias tab (rename labels for dimension/categorical legends), and Legend Order (default data order, and how to sort legend items by setting the sort order on the bound dimension field — ascending, descending, by value, or manual). Also explains how to hide and show legends.
endif::ai[]

[WARNING,caption=see also]
====
xref:dashboardscript:PlotProperties.adoc[]  in xref:dashboardscript:DashboardObjectReference.adoc[] to set these properties dynamically using script.
====

To edit a legend, right-click the legend, and select ‘Legend Properties’ from the context menu. {rightClickOptions} This opens the ‘Legend Properties’ panel, which provides the following tabs: *General*, *Scale*, *Alias*.

TIP: To hide a legend, right-click the legend and choose ‘Hide Legend’ from the context menu. {rightClickOptions} To show all legends, right-click the chart and select ‘Show Legend’ from the context menu.

For information on ordering legend items, see xref:#LegendOrder[] below.

== General Tab
The *General* tab offers the following options.

NOTE: {dynamicProperties}

image::Chart Legend Format Dialog.png[alt="legend properties panel"]

Title:: The text to display in the legend title bar.
Visible:: Sets the visibility of the legend title bar.
Legend Border:: The line style and color of the legend boundary.
Legend Position:: Sets the position of the legend on the plot. Legend position can also be changed by dragging with the mouse.
Ignore Null:: Ignore null values when generating the legend.
Symbol Size:: The size (in pixels) of symbols displayed in the legend (color swatches, shapes, textures, size bars, and line indicators). Valid range is 6–50. Default is 12.

== Scale Tab
The *Scale* tab offers the following options for *measure* (numerical) legends.

include::ROOT:partial$measureTip.adoc[]

Logarithmic Scale:: Set logarithmic scaling for the legend scale. (The logarithm base is selected automatically.)
Reverse:: Orient the scale with maximum at left, minimum at right (or maximum represented by smallest, minimum represented by largest, etc.).
Include Zero:: For a ‘Size’ legend, specifies that zero should be included in the scaling such that the (hypothetical) minimum element size corresponds to a value of zero rather than the actual minimum data value.



== Alias Tab
The *Alias* tab is available for *dimension* (categorical) legends, and displays a table containing the mapping between original legend labels and new legend labels.

include::ROOT:partial$dimensionTip.adoc[]

To modify the labels of a legend, double-click the cells in the ‘Alias’ column that correspond to the legend labels that you wish to rename, and enter the desired replacement text. Press btn:[OK] to close the panel.

image::legendFormatAlias.png[alt="double-click Alias column to rename labels"]

This updates the legend labels with the specified replacement text.

image::legendFormatAliasUpdated.png[alt="legend labels updated with replacement text"]

[#LegendOrder]
== Legend Order
By default, legend items are displayed in *data order*, the order in which values first appear in the data source. To change the order of legend items, set the sort order on the dimension field that is bound to the legend (i.e., the field assigned to the 'Color', 'Shape', or 'Size' region in the Chart Editor). Press the {buttonEditDimension} next to the field and choose from the following options in the 'Sort' menu:

[square]
* *Ascending* / *Descending*: Sort legend items alphabetically or in reverse-alphabetical order.
* *By Value (Asc)* / *By Value (Desc)*: Sort legend items by an aggregated measure value.
* *Manual*: Specify an arbitrary order using the 'Manual Ordering' dialog box.

See xref:GroupDataByDimension.adoc#SortRankChartGroups[Sort and Rank Chart Groups] for more details on these sort options.

NOTE: The *Reverse* option on the <<Scale Tab>> reverses the order of a *measure* (numerical) legend scale, not the item order of a categorical legend.