= Shape Properties
%experimental%
include::ROOT:partial$buttonSnippets.adoc[]


// metadata for AI
ifdef::ai[]
Summary: Reference for Shape component properties (Line, Oval, Rectangle). Covers border style and color, fill color and transparency, rounded corner radius (rectangles), gradient fills, and a drop shadow (Rectangle and Oval only, not Line). The Shadow controls live in a 'Shadow' group on the 'Shape' tab (not the General tab), and include an 'Apply' checkbox plus Color, Opacity, Direction (8 compass points), Distance, and Blur settings. See AddBackground for how to add shapes to a Dashboard.
endif::ai[]

[WARNING,caption=see also]
====
[square]
* xref:AddBackground.adoc[] for information on how to create a Shape.
* {commonProperties}
* xref:dashboardscript:Line.adoc[], xref:dashboardscript:Oval.adoc[], and xref:dashboardscript:Rectangle.adoc[]  in xref:dashboardscript:DashboardObjectReference.adoc[] to set these properties dynamically using script.

====

The Shape component’s ‘Properties’ dialog box provides *General*, *Shape*, and *Script* tabs. The next sections discuss the component-specific properties available under these tabs.

== General Tab
Only the common properties are provided on the *General* tab for Shape components.

== Shape Tab
The *Shape* tab in the ‘Properties’ dialog box for shape components provides slightly different properties for each shape type, as explained below.

TIP: {dynamicProperties}

image::Rectangle Properties Shape Tab.png[alt="shape tab line style and color properties"]

Line Style, Line Color::	Sets the line type (thickness, etc.) and color for shape borders. Available for all shapes.
Fill Color, Fill Alpha::	Sets the color and transparency of Oval and Rectangle shape interiors. An ‘Alpha’ value of 100% indicates complete opacity. An ‘Alpha’ value of less that 100% allows underlying components to be partially visible through the shape.
Shadow:: Adds a drop shadow to the component. Available for Rectangle and Oval; not available for Line.
+
image::shapeShadowSettings1.png[alt="shadow settings group on the shape tab with apply, color, opacity, direction, distance, and blur controls"]
+
Enable the ‘Apply’ checkbox to turn the shadow on, then set the following:
+
[square]
* *Color*: Sets the shadow color.
* *Opacity*: Sets the shadow's transparency, from 0 to 100%.
* *Direction*: Sets the direction of the shadow — North, Northeast, East, Southeast (default), South, Southwest, West, or Northwest.
* *Distance*: Sets how far the shadow is offset from the shape, in pixels (0-50).
* *Blur*: Sets the blur radius of the shadow's edge, in pixels (0-50).


Shape: Round Corners::	Sets the radius (in pixels) for circularized corners on Rectangle shapes. Larger pixel values yield more gentle corners.
Gradient::	Sets a gradient fill for Rectangle and Oval shapes. See xref:AddBackground.adoc[] for instructions on how to build a gradient.

== Script Tab
{commonScript}