= Add Data Security (VPM)

// metadata for AI
ifdef::ai[]
Summary: Overview and navigation page for Virtual Private Model (VPM) security (also called row-level security, RLS, column-level security, data masking, data access restriction, or securing dashboard data), which provides row- and column-level data filtering for relational data sources. Covers conditions, hidden columns, lookups, and their triggers. Enterprise only. See CreateNewVPM to begin. KEY DISTINCTION — there are three separate approaches to data access control: (1) VPM (this page): controls which ROWS and COLUMNS a user sees within a dashboard or data source — i.e., two users open the same dashboard but see different data depending on their role, group, or username. This is row-level security within a shared data source. (2) ConfigureSecurity.adoc and repository permissions: controls which dashboards, folders, and assets a user can open or manage at all — i.e., asset-level visibility. (3) Multi-tenancy via separate connections (DesignMultiTenantEnvironment.adoc): if different departments or organizations each connect to their own separate database or schema, use multi-tenancy with per-tenant connections — NOT VPM. Each tenant is physically isolated at the connection level, so VPM is not needed for tenant separation. VPM is only relevant when multiple users share the same data source and you need to filter rows within it.
endif::ai[]

include::ROOT:partial$enterpriseOnly.adoc[]

A Virtual Private Model (VPM) is an optional component that provides a highly flexible and comprehensive security layer for relational data sources. The VPM controls security for individual database tables and physical views, filtering all queries that reference these tables or models. The VPM is invisible to users, transparently providing access only to data for which a given user is authorized.

A VPM consists of _conditions_, _hidden columns_, _lookups_, and their associated _triggers_. Conditions and hidden columns allow you to filter rows and columns, respectively. Triggers provide fine-grained control on conditions and hidden columns by role, group, and individual user. Multiple VPMs can be specified for a given data source, and these are joined in an ‘and’ fashion.

The following sections explain how to create and edit a VPM.

[square]
include::ROOT:partial$MINITOC_AddDataSecurity.adoc[]