= Preview Data
include::ROOT:partial$buttonSnippets.adoc[]

// metadata for AI
ifdef::ai[]
Summary: How to preview the data returned by a Data Worksheet data block using Live Data View, showing a limited sample along with query execution time, how to enable or disable the display row limit, and how to export the previewed data to Excel.
endif::ai[]
include::ROOT:partial$rightClickOptions.adoc[]

To preview the data that will be returned by a data block, follow the steps below:


. Click on the data block to select it.

. In the bottom panel, press the {buttonChangeView}, and select {buttonLiveDataView}. This displays a limited preview of the data in the  data block.  The status bar also displays the *query execution time*.
+
image::agileTransform8.png[alt="press Change View and select Live Data View to preview data block"]
+
. To suspend the preview limit, press the {buttonChangeDataMode} and uncheck ‘Enable Display Row Limit’.
+
image::agileTransform9.png[alt="press Change Data Mode and uncheck Enable Display Row Limit to suspend preview limit"]
+
CAUTION: Removing the preview limit may slow the preview display. Note that even if you suspend the preview limit, the data set returned may still be limited by the administrator.
+
. To limit the data displayed, press {buttonChangeDataMode} again and check ‘Enable Display Row Limit’.
+
image::agileTransform12.png[alt="press Change Data Mode again and check Enable Display Row Limit to restore the limit"]
+
NOTE: The numerical value of the limit (maximum number of rows) is set by the administrator. The display row limit of a data block does not affect the results of operations, such as joins, in which the data block participates.

. *_Optional:_* To export the data displayed in the preview to an Excel file, press the {buttonExport} in the preview panel toolbar. This saves the data as an Excel file in the browser's default download location.




